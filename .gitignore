# Dependencies & build (repo-wide; subprojects have their own .gitignore)
node_modules/
dist/
*.local

# Rust / Cargo (workspace target lives under src/)
src/target/

# Tauri (desktop app generated files)
src/desktop/WixTools/
src/desktop/gen/
src/desktop/binaries/

# VibeAround job workspace (if working_dir ever points into repo)
projects.json
projects.json.tmp
workspaces/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Env & secrets
.env
.env.local
.env.*.local
src/settings.json

# Logs & test
*.log
npm-debug.log*
coverage/
.nyc_output/
logs/
tests/

# Temp & cache
*.tmp
*.temp
.turbo
.vite/
demo*/

# Tooling
*.tsbuildinfo

# Exclude from commit (tooling / temp)
.claude/
.specstory/
.docs/
.cursorindexingignore
.superpowers/

# Plugins (managed separately, not committed to main repo)
src/plugins/

# GitHub Wiki (独立仓库，本地编辑用)
wiki/

# key & build
apple-sign.config
build.sh
.build-env

# ts-rs leftover (removed — fence against accidental resurrection)
src/core/bindings/
